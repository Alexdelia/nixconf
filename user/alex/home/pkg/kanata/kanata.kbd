(defcfg
	danger-enable-cmd yes
)

(defalias
	esc-ctrl (tap-hold 100 100 esc lctrl)

	held-meta-layer (layer-while-held meta-layer)

	terminal (cmd kitty)
	browser (cmd brave)
)

(defsrc
	caps

	c
	b

	lmet
)

(deflayer default
	@esc-ctrl

	c
	b

	held-meta-layer
)

(deflay	meta-layer
	_

	@terminal
	@browser

	_
)
